---
name: project_corealign_design_system
description: 'CoreAlign frontend design system — brand=indigo, Tailwind @theme tokens, Phase 1 done, roadmap + pre-existing lint debt'
metadata:
  node_type: memory
  type: project
  originSessionId: 019f9014-1b3a-4366-b69c-3e5e25781406
---

CoreAlign frontend (D:\CoreAlign, React 19 + Vite 7 + Tailwind v4, FSD). As of 2026-06-16 I act as the responsible web designer.

**Canonical brand (decided by user):** product name **CoreAlign**, primary **indigo #6366f1**, accent **cyan #22D3EE**. Previously broken: 3 conflicting primaries (#3b82f6 / #0EA5E9 / indigo) and sidebar logo hardcoded the WRONG name **"NexusERP"** (now fixed to CoreAlign via `shared/ui/Logo`).

**Token architecture:** single source = Tailwind v4 `@theme` block in `src/index.css` with semantic ramps `primary`(indigo) / `accent`(cyan) / `success`(emerald) / `warning`(amber) / `danger`(red) / `info`(sky), each 50→950 → real utilities (`bg-primary-600`, `text-danger-600`). Neutral stays `slate-*`. White-label: `TenantThemeProvider` overrides `--color-primary-{400,500,600,700}` via `color-mix()` only when a custom tenant (`brandName`) is set — so components must use `primary-*` (not raw `indigo-*`) for tenant theming to reach them.

**Phase 1 DONE (non-breaking foundation):** @theme tokens; brand reconciliation (index.css + vite PWA theme_color + TenantTheme default → indigo); `Button`+`Input` migrated CSS-modules→token Tailwind (fixed ghost-hover light bug + dark placeholder bug, added focus-visible); Logo/Sidebar/Footer brand fix (deleted fake demo Footer w/ ONLINE_USERS+chat); deleted 6 dead CSS files (App.css + Button/Input modules + dead Sidebar/Navbar/DashboardLayout .module.css). Full plan + migration map in **`docs/DESIGN_SYSTEM.md`**.

**Phase 2a+2b+3a DONE (2026-06-16):** canonical primitives added — `shared/ui/{Label,Select,Textarea,Checkbox,Card}` + token-driven `Badge` + shared `shared/lib/fieldClasses.ts` + `shared/lib/statusStyles.ts` (generic `statusToneClass`; domain mappers go in features per FSD). **Color codemod APPLIED** (one-shot script in C:\Users\enes.colak\scratch\CoreAlign\migrate-colors.mjs): 317 files / 4403 replacements → semantic tokens (indigo/blue→primary, emerald/green→success, amber/yellow/orange→warning, rose/red→danger, sky→info). Lossless except intentional rose→red + blue→indigo; glass-enclosure `sky` kept as domain color. tone PROP names (FilterChip/PageHeader/DetailPanel) unchanged — only class-string values migrated, non-breaking. Updated stale color assertions in CapacityLoadView.test.tsx.

**Verified after each phase:** typecheck ✅, build ✅, test 206/206 ✅, edited files lint-clean.

**Phase 2c-part + 3b-start DONE (2026-06-16):** Added `shared/ui/PageTemplate` (`ListPageTemplate`/`DetailPageTemplate`, p-4 sm:p-6 + rhythm) and `shared/ui/Tooltip` (CSS hover, role=tooltip). Canonical component library now complete: Button, Input, Label, Select, Textarea, Checkbox, Card(+sub), Badge, Tooltip, PageHeader, PageTemplate, Modal(open prop, focus-trap), DataTable. Migrated EXEMPLAR pages to canonical pattern: `pages/settings/SettingsPage.tsx` + `pages/vendors/VendorsPage.tsx` (ListPageTemplate+PageHeader+Button/Input/Select/Textarea/Modal). All verified build+206 tests green. Page-pattern + exemplars documented in DESIGN_SYSTEM.md §7.

**Remaining admin-page migrations (~30, repetitive — apply VendorsPage/SettingsPage pattern):** accounting/_ (6), inventory/_, mrp/_ (3), reports/_ (4), purchasing/_ (PO/VendorBills/GoodsReceipts/3WayMatch/PayablesAging), warranty/_ (3), notifications, activity, admin/_ (4), Platform/Tenants/_ (2), orderTemplates/_, settings/{DiscountRules,ExchangeRates,Imports,PriceLists,TaxRules}, customers/CustomerDetailPage, vendors/VendorDetailPage, feedback. EXEMPT (no PageHeader): auth, legal/_, public/_ (landing=Phase 4), customer-portal/_ (own shell), glass designer, print views, *Section/*Form fragments.

**Phase 3b FULL + 2d DONE (2026-06-16):** ALL 40 admin pages migrated to canonical `ListPageTemplate`/`DetailPageTemplate` + `PageHeader` + primitives via background Workflow (corealign-page-canonicalize, 39 agents + TenantIdpAdminPage which the workflow wrote before an overload error). Conservative spec (scaffold-only, logic preserved). Central verify: typecheck clean, lint at pre-existing baseline (zero new), build ✅, test 206/206 ✅. NOTE: color codemod had lengthened some lines (rose→danger/sky→info) causing prettier violations repo-wide → fixed with `npx prettier --write src`. Phase 2d: removed both react-refresh eslint-disables by extracting contexts to `src/app/providers/themeContext.ts` (useTheme) and `src/app/theme/tenantThemeContext.ts` (useTenantTheme); updated 3 useTheme consumers (Navbar/AuthLayout/LandingPage). Many migrated pages introduced page-title i18n keys via t(...,{defaultValue}) that aren't yet in locale json (render correctly via fallback; sync later).

**Phase 2c shell DONE (2026-06-16):** shell surface `#0B0F19` → `@theme --color-shell` token (`dark:bg-shell`, scratch script tokenize-shell.mjs, 13 usages/10 files; dynamic JS bg values kept). Sidebar section headers i18n'd: added `Sidebar.sections.*` block to en.json (English) + tr.json (Turkish: Genel Bakış/Satış & CRM/…) via scratch script add-sidebar-i18n.mjs (string-insertion, no reformat); Sidebar.tsx renders sections via t() + SECTION_KEYS map. All verified green. NOT done (intentional): collapsible sidebar grouping (current section-divider pattern kept — clean/professional, collapsible is UX-preference), nav-item-name i18n (most have module keys already).

**Collapsible sidebar + FULL lint cleanup DONE (2026-06-16):** Sidebar rewritten to collapsible grouped sections (toGroups helper; section header toggles; active-route section auto-expands via isSectionOpen; icon-rail mode preserved; `cn` used). **`npm run lint` now fully GREEN (0 errors)** — fixed all 15 pre-existing: 4 react-hooks/set-state-in-effect (ThemeEditor/FxSourceSelector/useOnboarding via React "derive-during-render with prev-compare" pattern; LoginForm via lazy useState init from URL + side-effect-only effect), vite.config 3× any → ServerResponse, e2e import()-type → top `type Page`, mobile empty-interfaces → type aliases, mobile OfflineProvider unused enablePush, mobile AuthProvider/ThemeProvider react-refresh → extracted context+hook to authContext.ts/themeContext.ts (useAuth.ts re-export updated; consumers unaffected). All verified: eslint . exit 0, typecheck clean, build ✅, test 206/206 ✅. NOTE: the 4 set-state-in-effect refactors are behavior-preserving but lack unit tests — smoke-test login(SSO error)/theme-editor/fx-prefs/onboarding if touching.

**Landing enrichment + SEO DONE (2026-06-16):** SEO foundation — index.html rich meta (TR title/description/keywords) + Open Graph + Twitter card + JSON-LD SoftwareApplication + theme-color + favicon fix; public/og-image.svg (branded 1200x630), public/robots.txt, public/sitemap.xml; src/shared/lib/seo.ts (useSeo hook, upserts title/description/og/twitter) wired into LandingPage per-route (LANDING_SEO map). Hero enriched: dual CTA (primary + "Demo planlayın" outline → #demo) + trust line + entrance animations + animated end-to-end flow ribbon (Tasarım→Teklif→Üretim→Sevkiyat→Muhasebe) using new `ca-flow`/`animate-flow` keyframe in index.css. **All 13 landing section components enriched** via background workflow (corealign-landing-enrich): each got a bespoke animated inline-SVG visual/simulation (CAD schematic, MRP nesting/furnace, hub-spoke dealer net, balance scale, RFQ chart, SLA timeline, migration pipeline, security radar/shield, integration data-bus, knowledge graph, support pipeline, ROI before/after bars) + richer informative TR copy (t() defaultValue) + semantic h2/h3 + aria. Pre-launch honest (no fake customers). Verified: typecheck ✅, `eslint .` 0, build ✅, test 206/206 ✅. NOTE: many new landing i18n keys use t() defaultValue (render correctly; backfill to locale json for full translation later).

**Landing i18n backfill DONE (2026-06-16):** Extracted all 330 new landing `t('LandingPage.*', { defaultValue })` keys across 15 files (workflow corealign-landing-i18n-extract, agents returned {key, tr, en}); merged 266 NEW keys (64 already existed, 0 conflicts) into src/app/i18n/locales/tr.json (Turkish) + en.json (English) via add-only deep-merge script (C:\Users\enes.colak\scratch\CoreAlign\merge-from-output.mjs, decodes HTML entities, never overwrites existing). en.LandingPage 401→667 leaf keys, symmetric with tr. Verified: EN now renders English (not TR fallback), entities clean (& not &amp;), build ✅, test 206/206 ✅. defaultValue kept in components as harmless safety net (locale values take precedence). ar/de/ru still fall back to en (consistent w/ existing pattern; full 5-lang would be separate task).

**SEO adversarial audit + safe fixes (2026-06-16):** 7-lens adversarial audit (workflow corealign-seo-audit) scored landing **31/100** (crawl 22, i18n 24, meta 28, social 32, semantics 32, structured 38, cwv 42; 19 blockers). Fixed the SAFE/in-scope items (verified green): JSON-LD expanded to @graph (SoftwareApplication + Organization + WebSite + **FAQPage** built from tr.json faq, 9 Q&A) via scratch inject-jsonld.mjs; dynamic `<html lang>` on locale change (i18n config); og:locale:alternate; meta description tightened to ~155; useSeo extended with per-route canonical + og:url. **Remaining BLOCKERS need architectural decisions (NOT done):** (1) SPA has no SSR/prerender → one HTML shell for all routes, invisible to non-JS crawlers/social scrapers — biggest lever, needs vite prerender/SSG; (2) og:image is SVG — social platforms require PNG/JPG 1200x630 (no rasterizer in env); (3) no URL-based locale (/tr,/en) → no real hreflang / dual-language indexing (app-wide router+i18n change); (4) placeholder domain corealign.com; (5) CWV — landing may pull heavy app chunks (three.js, country-state-city). So landing SEO is NOT "perfect" — ceiling set by these 4-5 architectural items.

**SEO blockers RESOLVED + re-audited (2026-06-16):** Re-audit scored **83/100** (from 31), then fixed all remaining code-level findings: (1) CWV blocker — stripped wasteful modulepreloads (vendor-3d 1.3MB + vendor-charts 392KB + ar/de/ru locales, ~1.85MB) from prerendered landing HTML (kept vendor-forms — LoginForm uses RHF); (2) og:locale:alternate now per-lang (TR→en_US, EN→tr_TR); (3) JSON-LD removed non-standard offers.category + applicationSubCategory; (4) added og/twitter:image:alt. Domain now single env var: `SITE_URL=https://x npm run build` rewrites ALL urls (canonical/og/hreflang/JSON-LD/sitemap) via prerender global replace (default corealign.com). Deps: added `@resvg/resvg-js` (devDep) for OG PNG. Scripts: scripts/gen-og-image.mjs (`npm run gen:og`), scripts/prerender-seo.mjs (`npm run prerender`, wired into build). Verified: build+prerender ✅, eslint . 0 ✅, test 206/206 ✅, dist greps confirm each fix. Effective SEO now mid-90s; literal 100 needs live deploy (real domain via SITE_URL + host serving dist/<route>/index.html per path, not blanket SPA-fallback) + non-applicable extras (SearchAction needs a real search). All code-level audit findings closed.

**SEO confirmation audit + perfection fixes (2026-06-16):** 3rd audit scored low (65, crawl 15) for TWO reasons: (a) one crawl agent HALLUCINATED "prerender/sitemap/og missing" — contradicted by direct ground-truth (all present); (b) my own standalone `npm run prerender` re-runs + a SITE_URL=app.corealign.io test had POLLUTED dist/index.html (the shell == TR-home output), causing dup hreflang (3→12), app.io domain leak in og:image/JSON-LD, and frozen TR snapshot on all pages. ROOT FIX: made prerender-seo.mjs IDEMPOTENT (strips prior hreflang + resets #root via data-prerendered marker on shell read) + generates per-lang JSON-LD @graph (Organization ImageObject logo + @id linking + BCP47 inLanguage + removed non-standard offers; FAQPage from loc[lang].faq so EN pages get English Q&A) + per-lang og:image:alt/twitter:image:alt + og:type=article for /articles + shortened home titles ≤60 + sitemap lastmod. Clean `vite build`+prerender VERIFIED in dist: hreflang=3, app.io=0, titles 31-51 chars, per-lang h1 (EN="Central Cloud Intelligence…"), EN FAQPage English, ImageObject logo. test 206/206 ✅, eslint . 0 ✅. Lesson: don't run standalone `npm run prerender` repeatedly on dirty dist; always clean `vite build` first (now idempotent anyway). NOTE: `tsc`/`npm run build` currently RED due to user's concurrent WIP src/pages/accounting/YearEndClosePage.tsx (imports useFiscalYearCloseStatus/useOpenFiscalYear not exported by useFiscalYearCloseQueries) — unrelated to SEO; vite build (esbuild) unaffected. Genuinely-remaining (NOT code): real domain (SITE_URL env), host serving dist/<route>/index.html per-path, twitter:site/creator handle, Organization sameAs, SearchAction (needs real search endpoint).

**Landing layout coherence pass (2026-06-16):** User reported landing "scattered, inconsistently centered, layers independent." Diagnosis: hero LEFT-aligned + no max-width vs sections `text-center` + `max-w-5xl mx-auto`; inconsistent padding (hero py-16, most py-20, Solutions px-6 py-12); random ad-hoc section bg bands (bg-white/20, bg-slate-100/50, border-t) on ~4 sections; asymmetric layout (fixed login sidebar left) made centered text look off. FIX: new `src/pages/public/components/Section.tsx` (Section: unified px-6 sm:px-10 lg:px-16 + py-16 sm:py-20 + mx-auto max-w-5xl + subtle border-t divider; SectionHeader: LEFT-aligned eyebrow pill + h2 + subtitle, max-w-2xl). Hero + footer wrapped in same max-w-5xl container + padding. Workflow (corealign-landing-coherence) refactored all 15 sections to Section/SectionHeader: left-aligned intros (removed text-center), removed ad-hoc section bg, kept card internals + SVG visuals + interactivity. Verified: 15/15 adopt Section, no centered intros, typecheck clean (only user's unrelated YearEndClosePage WIP errors), eslint . 0, test 222/222, vite build ✅. Minor follow-ups (benign): eyebrow pills lost backdrop-blur (now uniform); FaqSection dropped aria-labelledby (SectionHeader has no id prop — h2 still visible). View via `npm run dev` (npm run build blocked by user's YearEndClosePage tsc WIP).

**Typography + section-transition polish (2026-06-16):** User: "fonts, transitions between topics, design can be much more developed." (1) Typography: installed `@fontsource-variable/inter` (self-hosted, Turkish glyphs), imported in main.tsx; index.css font-family now 'Inter Variable' first (was Inter→system-ui fallback, never actually loaded) + font-optical-sizing:auto + font-feature-settings 'cv11','ss01'; added @layer base: h1-h3 text-wrap:balance + letter-spacing:-0.02em, p text-wrap:pretty. (2) Section transitions: new src/shared/hooks/useInView.ts (IntersectionObserver, once, lazy-init for no-IO so no set-state-in-effect); Section.tsx now scroll-reveals its container (fade+translate-y, motion-reduce safe) + replaced hard border-t with an edge-fading gradient hairline divider → smooth flow between topics. (3) SectionHeader refined: larger responsive h2 (text-3xl sm:text-4xl lg:text-[2.75rem]), tighter leading/tracking, balanced wrapping, refined eyebrow/subtitle spacing. Cascades to all 15 sections + whole-app font. Verified: eslint . 0, test 231/231, vite build ✅, typecheck clean (only user's YearEndClose WIP). Design is iterative — invited user feedback for deeper per-section visual richness.

**Other remaining:** Phase 2c shell (Sidebar IA grouping + i18n keys [common.close/toggleSidebar, Footer.rights/tagline currently via defaultValue], shell surface `#0B0F19`→token, CommandPalette grouping, Tooltip primitive, card consolidation), 2d theme eslint-disable hook-extraction, 3b ListPageTemplate/DetailPageTemplate + migrate non-canonical pages (Vendors/Inventory/Settings/accounting), 3c raw form controls→primitives, Phase 4 landing/login. Locale files: src/app/i18n/locales/{en,tr,ar,de,ru}.json (5694 lines, fallback=en).

**Pre-existing lint debt (NOT mine, repo `npm run lint` already red):** 17 errors — `mobile/` app (5), `e2e/`, and `react-hooks/set-state-in-effect` in LoginForm/FxSourceSelector/useOnboarding/ThemeEditor + `@typescript-eslint/no-explicit-any` in vite.config.ts proxy handler.

**Roadmap (see doc):** Phase 2 shell (sidebar IA grouping + i18n, shell surface tokens `#0B0F19`→token, add Select/Textarea/Checkbox/Tooltip primitives, card consolidation, remove 2 react-refresh eslint-disables in ThemeProvider/TenantThemeProvider via hook extraction). Phase 3 raw-color→semantic-token codemod (~10,542 occurrences, 308 files; rose→red merge) via worktree-isolated parallel agents. Phase 4 landing/marketing + login polish. Audit baseline: 10,542 hardcoded color utils, 70% of .tsx files. Relates to [[project_corealign_notification_integration]].
